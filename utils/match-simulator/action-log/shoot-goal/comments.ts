import { GOAL_SCORE } from '@utils/constants/game';

export const comments = [
  `{{shooter.name}} shoots... and it's in! It's a GOAL for {{shooter.team.name}}`,
  `A great shoot by {{shooter.name}} that {{keeper.name}} can't reach! It's GOAL! ${GOAL_SCORE} more points for {{shooter.team.name}}`,
];
