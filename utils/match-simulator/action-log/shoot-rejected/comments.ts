export const comments = [
  `{{shooter.name}} shoots... and the keeper rejects it. Good intervention of {{keeper.name}} but the ball is still in play`,
  `A shoot by {{shooter.name}} that gets rejected by {{keeper.name}}. Ball still in play`,
];
